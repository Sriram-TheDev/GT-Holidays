// src/hooks/usePackages.js
// ─────────────────────────────────────────────────────────────────
// AGENT PLACEHOLDER — Agent-4 (Discover) must implement this.
// ─────────────────────────────────────────────────────────────────
// PURPOSE:
//   const { packages, loading, error } = usePackages(filters?)
//   - Fetches packages collection from Firestore
//   - Supports optional filter object { type, maxPrice, tags[] }
//   - Returns sorted data ready for DiscoverPage
//
// STUB EXPORT:
export function usePackages() {
  return { packages: [], loading: true, error: null }
}
