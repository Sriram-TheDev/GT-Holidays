// src/hooks/useScrollTrigger.js
// ─────────────────────────────────────────────────────────────────
// AGENT PLACEHOLDER — Agent-5 (PackageDetails) must implement this.
// ─────────────────────────────────────────────────────────────────
// PURPOSE:
//   useScrollTrigger(triggerRef, options)
//   - Creates a GSAP ScrollTrigger tied to triggerRef element
//   - Returns { progress, isActive, isPinned }
//   - Used by the sticky condensed header on /package/:id
//
// STUB EXPORT:
export function useScrollTrigger() {
  // Implement using gsap.ScrollTrigger — see ARCHITECT_PLAN.md
}
