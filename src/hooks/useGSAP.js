// src/hooks/useGSAP.js
// ─────────────────────────────────────────────────────────────────
// AGENT PLACEHOLDER — Agent-4 (Discover) or Agent-5 (PackageDetails)
// must implement this hook.
// ─────────────────────────────────────────────────────────────────
// PURPOSE:
//   useGSAP(animateFn, deps, scopeRef?)
//   - Runs animateFn inside a GSAP context scoped to scopeRef
//   - Automatically cleans up on unmount
//   - Respects prefers-reduced-motion
//
// REFERENCE: @gsap/react useGSAP hook (already in package.json)
// Use the official @gsap/react useGSAP instead of this placeholder.
//
// STUB EXPORT (safe to import while empty):
export function useGSAP() {
  // Implemented via @gsap/react — see ARCHITECT_PLAN.md
  // Do not build a custom implementation here
}
